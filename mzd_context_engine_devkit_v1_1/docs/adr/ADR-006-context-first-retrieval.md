# ADR-006：具体事实只从 ContextPack 检索

状态：Accepted

## 背景

通用模型记忆无法保证选定版本、时间边界和具体事实稳定。

## 决策

生成具体历史内容时，只允许使用当前正文、已审核 ContextUnit 和来源摘录。资料不足返回 unavailable。模型记忆只能用于语言组织和抽象推理，不得新增日期、数字、人物关系或地点。

## 后果

内容生产成为重要成本，但准确性、可追踪性和可修订性显著提高。

