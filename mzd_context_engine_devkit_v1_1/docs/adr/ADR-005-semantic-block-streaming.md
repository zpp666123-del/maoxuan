# ADR-005：采用语义块流式而非裸 Token 流

状态：Accepted

## 背景

裸 Token 流感知速度快，但错误事实可能先展示，结构化输出难以验证，卡片容易半成品覆盖。

## 决策

按 headline、summary、point 等语义块生成和局部校验；完整卡片通过所有门禁后发送 committed 事件，作为最终真相。

## 后果

首字速度略慢，但内容更稳定、可回滚、可审查，前端状态更简单。

