# ADR-003：固定框架实时流式生成

状态：Accepted

## 背景

产品只保留一种阅读解释路径：用户进入文章、切换锚点、点击段落、选中文本或提问后，由预制三槽位框架约束真实大模型实时生成流式输出。

## 决策

不再维护静态解释快路径、离线兜底答案或伪生成内容。正文始终可读；若模型配置缺失或模型不可用，系统通过 `interaction.failed` 告知，而不是用旧内容冒充实时生成。

## 后果

系统重点转为模型配置、提示词约束、流式协议、运行记录、来源校验和失败可观测性。成本与延迟通过锚点切分、最小上下文、并行槽位和超时控制解决。
