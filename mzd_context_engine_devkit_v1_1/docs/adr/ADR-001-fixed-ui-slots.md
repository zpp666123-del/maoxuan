# ADR-001：模型不得决定页面布局

状态：Accepted

## 背景

自由生成页面容易造成界面不稳定、信息重复、内容过长和前端难以测试。

## 决策

产品预先定义背景、处境、思想与条件式场景四类槽位。模型只能输出符合 Schema 的内容和场景类型，不得生成 HTML、CSS、SVG 或任意组件树。

## 后果

优点：稳定、可测试、可流式、可审查。代价：需维护槽位 Prompt 与 Schema，部分复杂问题要通过展开或问答处理。

